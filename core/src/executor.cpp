#include <executor.hpp>

#include <string>
#include <vector>

namespace coreutils {

int Executor::runCommands(std::vector<CommandPtr> cmds, Input& in, Output& out) {
  // TODO
  return {};
}

}  // namespace coreutils
